// Radix DirectionProvider — sets text direction (ltr/rtl) for all Radix
// primitives nested under it. Wrap your app/subtree to enable RTL.
export { DirectionProvider } from '@radix-ui/react-direction';
